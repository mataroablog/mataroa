# Server connection details
export SERVER_IP=
export SERVER_USER=root

# Main domain name
export DOMAIN=example.com

# Admin email for server error alerts
export ADMIN_EMAIL=

# Show exceptions and tracebacks on errors
# 1: show
# 0: don't show
export DEBUG=1

# Stop real emails and turn https off
# 1: stop and off
# 0: do not stop and on
export LOCALDEV=1

# Session cookies secret
export SECRET_KEY=some-secret-key

# Database connection
export DATABASE_URL=postgres://mataroa:xxx@localhost:5432/mataroa
export POSTGRES_USERNAME=mataroa
export POSTGRES_PASSWORD=xxx

# SMTP credentials
export EMAIL_HOST_USER=
export EMAIL_HOST_PASSWORD=

# Custom domain IP
export CUSTOM_DOMAIN_IP=

# Stripe payments details
export STRIPE_API_KEY=
export STRIPE_PUBLIC_KEY=
export STRIPE_WEBHOOK_SECRET=
export STRIPE_PRICE_ID=

# Backup and rclone configuration
export BACKUP_BUCKET=
export RCLONE_PROVIDER=
export RCLONE_ACCESS_KEY_ID=
export RCLONE_SECRET_ACCESS_KEY=
export RCLONE_REGION=
export RCLONE_ENDPOINT=
